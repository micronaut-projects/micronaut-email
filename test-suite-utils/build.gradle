plugins {
    id 'java-library'
}

repositories {
    mavenCentral()
}

dependencies {
    api(libs.javax.mail)
    api(libs.poi.ooxml)
}

java {
    sourceCompatibility = JavaVersion.toVersion("1.8")
    targetCompatibility = JavaVersion.toVersion("1.8")
}
