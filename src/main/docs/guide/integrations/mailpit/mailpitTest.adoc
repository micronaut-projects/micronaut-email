You can use Mailpit and the <<mailpitHttpClient, Micronaut Mailpit HTTP Client>> to test the logic of your application responsible of sending emails.

The following test verifies the behaviour of `OrderService` bean which was shown in the <<mailpitDev, Mailpit for Development>> section running Mailpit via Testcontainers.

snippet::io.micronaut.email.mailpit.client.OrderServiceTest[]
